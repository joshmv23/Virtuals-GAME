import { BaseRequestParams } from './types';
export declare function getBaseRequestParams(requestParams: BaseRequestParams): {
    initParams: RequestInit;
    baseUrl: string;
};
export declare function generateRequestId(): string;
export declare function makeRequest<T>({ url, init, requestId, }: {
    url: string;
    init: RequestInit;
    requestId: string;
}): Promise<T>;
