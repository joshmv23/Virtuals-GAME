/**
 * Creates and signs the transaction.
 * @param {any} gasLimit - The gas limit for the transaction.
 * @param {any} amount - The amount to transfer.
 * @param {any} gasData - Gas data (maxFeePerGas, maxPriorityFeePerGas, nonce).
 * @returns {Promise<string>} The signed transaction.
 */
export declare const createAndSignTransaction: (tokenIn: string, recipientAddress: string, amount: any, gasLimit: any, gasData: any, chainId: string, pkpPublicKey: string) => Promise<any>;
//# sourceMappingURL=create-and-sign-tx.d.ts.map