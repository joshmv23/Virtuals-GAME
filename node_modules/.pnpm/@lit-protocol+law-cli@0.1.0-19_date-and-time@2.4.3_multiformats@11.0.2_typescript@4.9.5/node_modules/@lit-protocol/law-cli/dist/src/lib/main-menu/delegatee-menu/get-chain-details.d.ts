import { LocalStorage } from '../../core';
export interface ChainInfo {
    name: string;
    chainId: number;
    rpcUrls: string[];
}
/**
 * Prompts the user to select a chain from the configured chains or enter custom values.
 */
export declare const promptSelectChain: (localStorage: LocalStorage, params: {
    needsChainId?: boolean;
    needsRpcUrl?: boolean;
}) => Promise<{
    chainId?: string;
    rpcUrl?: string;
}>;
//# sourceMappingURL=get-chain-details.d.ts.map