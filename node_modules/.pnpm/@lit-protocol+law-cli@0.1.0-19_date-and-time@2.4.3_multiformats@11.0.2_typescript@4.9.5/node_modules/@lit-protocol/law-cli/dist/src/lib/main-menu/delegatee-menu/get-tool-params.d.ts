import { AwTool } from '@lit-protocol/agent-wallet';
import { LocalStorage } from '../../core';
/**
 * Prompts the user to input parameters for a tool.
 */
export declare const getToolParams: <T extends Record<string, any>>(localStorage: LocalStorage, tool: AwTool<T, any>, pkpEthAddress: string, options?: {
    missingParams?: Array<keyof T>;
    foundParams?: Partial<T>;
}) => Promise<T>;
//# sourceMappingURL=get-tool-params.d.ts.map