"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.handleGetDelegatees = void 0;
const core_1 = require("../../../core");
const handleGetDelegatees = async (admin, pkp) => {
    const delegatees = await admin.awAdmin.getDelegatees(pkp.info.tokenId);
    if (delegatees.length === 0) {
        core_1.logger.info('No delegatees found.');
        return;
    }
    core_1.logger.info('Current Delegatees:');
    delegatees.forEach((address) => {
        core_1.logger.log(`  - ${address}`);
    });
};
exports.handleGetDelegatees = handleGetDelegatees;
