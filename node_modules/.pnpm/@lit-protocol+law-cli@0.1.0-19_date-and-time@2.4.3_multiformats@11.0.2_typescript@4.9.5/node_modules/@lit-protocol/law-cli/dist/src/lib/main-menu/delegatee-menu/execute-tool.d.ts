import { type DelegatedPkpInfo } from '@lit-protocol/agent-wallet';
import { Delegatee } from './delegatee';
import { LocalStorage } from '../../core';
/**
 * Handles the process of executing a tool.
 * This function displays available tools, prompts for tool selection and parameters,
 * and executes the selected tool with the provided parameters.
 */
export declare const handleExecuteTool: (localStorage: LocalStorage, delegatee: Delegatee, pkp: DelegatedPkpInfo) => Promise<void>;
//# sourceMappingURL=execute-tool.d.ts.map